[mgr]
show_hidden = true

[preview]
tab_size = 2

[opener]
open = [
  { run = "$EDITOR %s", block = true, for = "unix" },
]
edit = [
  { run = "$EDITOR %s", block = true, for = "unix" },
]
pdf = [
  { run = "zathura %s", desc = "Open with zathura" },
]

[open]
rules = [
  { name = "*/", use = [ "edit", "open" ] },
  { mime = "text/*", use = [ "edit" ] },
  { mime = "application/pdf", use = [ "pdf" ] },
  { name = "*", use = [ "open" ] },
]
