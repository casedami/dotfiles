install homebrew

```console
curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh | bash
brew bundle install
```

then symlink everything to XDG_CONFIG_HOME
```console
./link.nu
```
