/* Vertical centering of content within notification */
.notification-content {
  display: flex;
  flex-direction: column;
  justify-content: center;
}

/* Center the text within the notification body */
.notification-body {
  display: flex;
  align-items: center;
  min-height: 3em; /* Ensure minimum height for proper centering */
}

/* Center summary text (notification title) */
.summary {
  display: flex;
  align-items: center;
}

/* Center body text (notification message) */
.body {
  display: flex;
  align-items: center;
}
